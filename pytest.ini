[pytest]
# Bare `pytest` (as CI runs it) does not put the repo root on sys.path,
# so `import src` fails without this.
pythonpath = .
markers =
    integration: tests that require real models/GPU/network (deselected by default)
addopts = -m "not integration"
