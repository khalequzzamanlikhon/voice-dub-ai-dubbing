# Hugging Face token — required for pyannote.audio (gated models: pyannote/speaker-diarization-3.1)
HF_TOKEN=

# OpenAI API key — only required if TRANSLATION_BACKEND=gpt
OPENAI_API_KEY=

# Optional: override default device (cuda / cpu / mps)
DEVICE=cuda
